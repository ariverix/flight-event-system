# Semgrep ignore (P4-4 CI security-scan).
# Семантика: дополняет встроенные дефолтные паттерны semgrep (тесты, минифицированные
# бандлы, лок-файлы и т.п.), не заменяя их — здесь только точечные проектные исключения.

# Сид дефолтного администратора (admin/admin) для демо/первичной инициализации.
# Поле password_hash — это НЕОБРАТИМЫЙ BCrypt-хэш строки "admin", а не секрет:
# хэш нельзя «расшифровать», утечки учётных данных нет. Правило
# generic.secrets.security.detected-bcrypt-hash здесь — ложноположительное.
# Файл — ПРИМЕНЁННАЯ Flyway-миграция, править её нельзя (checksum), поэтому
# исключаем на уровне сканера, а не правкой SQL.
backend/src/main/resources/db/migration/V8__insert_default_admin.sql
