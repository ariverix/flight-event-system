spring:
  application:
    name: eca-system

  datasource:
    url: jdbc:postgresql://localhost:5432/eca_db
    username: eca_user
    password: eca_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  modulith:
    events:
      jdbc:
        schema-initialization:
          enabled: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  info:
    env:
      enabled: true

info:
  app:
    name: ECA System
    description: Event-Condition-Action system for aviation message processing
    version: 1.0.0

logging:
  level:
    ru.protectinfotrans.eca: DEBUG
    org.springframework.modulith: INFO

app:
  jwt:
    secret: eca-jwt-secret-key-for-development-minimum-256-bits-long
    expiration-ms: 86400000
