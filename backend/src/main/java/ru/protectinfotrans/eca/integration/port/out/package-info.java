/**
 * Внутренние выходные порты модуля Integration Adapter (P2-3: durable исходящий шлюз).
 * В отличие от {@code integration.port} (видим снаружи), этот пакет НЕ является
 * Named Interface — реализуется и используется только внутри модуля {@code integration}.
 */
package ru.protectinfotrans.eca.integration.port.out;
