/**
 * Сервисы модуля Custom Fields Engine: {@code CustomFieldRuleService} (CRUD правил),
 * {@code CustomFieldExtractionService} (извлечение значений из входящих + закрытие per-flight
 * контекста при завершении рейса), {@code CustomFieldQueryService} (чтение активных значений
 * для подстановки в шаблоны/критерии).
 */
package ru.protectinfotrans.eca.customfields.application;
