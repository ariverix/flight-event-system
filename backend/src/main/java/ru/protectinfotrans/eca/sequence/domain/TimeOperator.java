package ru.protectinfotrans.eca.sequence.domain;

/**
 * Оператор критерия TIME_COMPARISON — паритет с SITA Sequencer (is before/is equal to/is after).
 */
public enum TimeOperator {
    IS_BEFORE,
    IS_EQUAL,
    IS_AFTER
}
