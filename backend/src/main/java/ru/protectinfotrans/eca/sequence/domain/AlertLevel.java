package ru.protectinfotrans.eca.sequence.domain;

/**
 * Уровень алерта для RAISE_CONDITION — паритет с SITA Sequencer (No/Low/Medium/High/Critical).
 */
public enum AlertLevel {
    NO,
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
