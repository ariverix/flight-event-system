package ru.protectinfotrans.eca.sequence.domain;

/**
 * Происхождение шаблона uplink-сообщения — паритет с SITA Sequencer.
 * COMPUTER_GENERATED — сформировано автоматически системой по шаблону.
 * EXTERNAL_USER — введено вручную внешним пользователем (диспетчером) по шаблону.
 */
public enum UplinkOrigin {
    COMPUTER_GENERATED,
    EXTERNAL_USER
}
