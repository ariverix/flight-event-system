package ru.protectinfotrans.eca.sequence.domain;

/**
 * Единица измерения длительности для ACTION-шага "wait for {x} {sec/min/hour}" — паритет с SITA.
 */
public enum WaitTimeUnit {
    SEC,
    MIN,
    HOUR
}
