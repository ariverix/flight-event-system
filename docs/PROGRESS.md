# PROGRESS — ledger выполнения RUN_PLAN.md

> Ведётся автономно: после каждой закрытой задачи добавляется/обновляется
> строка. Источник формулировок задач — `RUN_PLAN.md`. Не переписывать
> историю прошлых строк задним числом — только добавлять новые записи
>/менять статус и колонку "Доказательство" по факту.
>
> Статусы: `Pending` → `In progress` → `Done` (или `Blocked`, если задача
> застряла — с указанием причины в "Доказательство").

## P0 — Фундамент

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P0-1 | CI/CD каркас в GitHub Actions: build → `./gradlew test` → JaCoCo coverage gate ≥85% → шаг Modulith-verify | devops-agent + architect | Done | Коммит `42aabec` ("P0 guardrails"), reviewer PASS. JaCoCo gate baseline LINE≥0.88 / INSTR≥0.90, Modulith-verify в CI. |
| P0-2 | Тест границ модулей `ApplicationModules.of(App).verify()`, починка нарушений | architect + backend-dev | Done | Коммит `42aabec`, reviewer PASS. Тест зелёный, нарушений границ не найдено. |
| P0-3 | springdoc + OpenAPI на текущее API + Swagger UI | docs-agent | Done | Коммит `42aabec`, reviewer PASS. `docs/openapi/openapi.json` сгенерирован, Swagger UI подключён. |
| P0-4 | Структурные JSON-логи + correlationId сквозным фильтром; каркас `audit_log` (новая миграция) | observability-agent + db-dev | Done | Коммит `42aabec`, reviewer PASS. Spring Boot 3.5 ECS JSON-логи, correlationId-фильтр с whitelist-валидацией, миграция V20 (`audit_log.correlation_id`). |
| P0-5 | `docs/adr/` + ADR-0001 «модульный монолит vs микросервисы»; `docs/PROGRESS.md` ledger | architect + tech-lead | Done | reviewer PASS. `docs/adr/ADR-0001-*.md`, `ADR-template.md`, `README.md`, `docs/PROGRESS.md`. Коммит завершения P0. |
| P0-6 | Threat model (черновик) + чек-лист требований ФСТЭК + license report зависимостей в CI | security-agent + compliance-agent | Done | reviewer PASS. `docs/security/threat-model.md` (STRIDE, R-1..R-9), `docs/security/fstec-checklist.md`, license-maven-plugin (`aggregate-third-party-report`) + CI artifact. Коммит завершения P0. |

## P1 — Промышленное ядро движка

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P1-1 | Паритет SITA: 3 типа шагов (ACTION/EVALUATE/WAIT), 6 типов критериев с операторами и AND/OR | sequence-engine-dev | Done | reviewer PASS (1 цикл bug-fixer: position_source fallback). Все типы шагов/критериев, операторы, AND/OR nested, from-this-point-only, estimated-ignore. Миграция V21. 292 теста зелёные. |
| P1-2 | Решения CONTINUE/GOTO/END/ABORT (true/false) + Notify; start/stop-критерии непрерывной оценки | sequence-engine-dev | Done | reviewer PASS. Независимые true/false решения, GOTO вперёд/назад, Notify, start/stop непрерывной оценки (схема V2/V3 уже поддерживала). Починены 2 бага: бесконечный синхронный GOTO-цикл (loop guard 1000→ABORT), устаревший WAIT-таймаут при повторном входе. 307 тестов. |
| P1-3 | Персистентный стейт инстанса: таблица `sequence_instance` + миграция + репозиторий | db-dev + sequence-engine-dev | Done | reviewer PASS. `execution_instances`(=sequence_instance) уже имела шаг/статус/таймауты/context; V22 +updated_at +version(nullable, задел P1-6). Механизм InstanceContext/Codec, save-on-every-transition. Починен баг: from-this-point-only reference переживает очистку waitStartedAt и рестарт (готово к P1-4). OpenAPI обновлён. 321 тест. |
| P1-4 | Resume после рестарта незавершённых инстансов | sequence-engine-dev + test-engineer | Done | reviewer PASS (1 цикл bug-fixer: транзакционная изоляция — REQUIRES_NEW на инстанс, reload-by-id от detached/LazyInit, тест с PG-триггером). `ExecutionResumeRunner` (ApplicationRunner на ApplicationReadyEvent) сканирует RUNNING/WAITING при старте; WAITING восстанавливается "бесплатно" (WAIT-окно и from-this-point-only уже персистентны с P1-3, читаются заново слушателем/scheduler); RUNNING докручивается повторным детерминированным прогоном текущего шага (`ExecutionService#resumeRunningInstanceAfterRestart`). Гарантия сейчас — at-least-once (не exactly-once для ACTION с внешним эффектом) — усиливается Outbox в P1-7. Без миграции (V22 уже всё нужное персистит). Multi-replica/leader election — зона P6-1, не реализовано. 337 тестов.
| P1-5 | Durable-планировщик WAIT/таймаутов (БД-backed) | sequence-engine-dev | Done | reviewer PASS. Single-fire через атомарный условный UPDATE-claim (`wait_timeout_at=NULL WHERE id AND status AND wait_timeout_at=expected`), REQUIRES_NEW, без ShedLock/Quartz и без миграции (импортозамещение). WaitTimeoutScheduler (@Scheduled, тонкий). Переживает рестабт. Конкурентный тест 8 потоков на реальном Postgres. leader election не нужен для корректности. 343 теста. |
| P1-6 | Конкурентность: оптимистические блокировки, без гонок на много инстансов | sequence-engine-dev | Done | reviewer PASS. `@Version` на ExecutionInstance (колонка из V22, без новой миграции); bounded-retry (5) на ObjectOptimisticLockingFailureException с перечитыванием в REQUIRES_NEW; фан-аут event→много инстансов: per-instance REQUIRES_NEW через self-proxy (снята классовая @Transactional). Конкурентные микротесты 10-25 инстансов/потоков на реальном Postgres. 349 тестов. |
| P1-7 | Transactional Outbox (`event_publication`) + идемпотентный приём + ADR Outbox vs прямой вызов | architect + sequence-engine-dev + db-dev | Done | reviewer PASS. ADR-0002 (Outbox через Spring Modulith Event Publication Registry vs прямой вызов; исключение ActionStepRule→MessageOutputPort синхронно). Флаги republish-on-restart + completion-mode=update (сверены с metadata 1.3.1). Идемпотентность startExecution через V23 `triggering_message_id` + dedup-индекс. 8 тестов (republish через настоящий Modulith API, dedup, atomicity). 361 тест. Follow-up: см. backlog. |
| P1-8 | Event Log класса Tracking: завершение шага / старт-стоп последовательности | observability-agent | Done | reviewer PASS. V24: `sequences.logging_enabled` (DEFAULT TRUE) + таблица `tracking_event_log` (+4 индекса). Запись SEQUENCE_STARTED/STEP_COMPLETED/SEQUENCE_STOPPED/SEQUENCE_ABORTED в ExecutionService, gated по флагу (через SequenceQueryPort, границы целы), в одной tx с переходом, correlationId. Идемпотентность опирается на P1-6/P1-7. 375 тестов. |

## P2 — Интеграция ACARS

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P2-1 | Входящий шлюз ACARS: приём → нормализация → `MessageReceived`, идемпотентность по ID сообщения | integration-dev + db-dev | Done | reviewer PASS (1 цикл bug-fixer: TOCTOU-гонка). Идемпотентность шлюза по `externalMessageId` (persist-before-process, find-before-save), V25 `messages.external_message_id` + partial UNIQUE. Гонка: saveAndFlush+catch DataIntegrityViolation+recovery-read REQUIRES_NEW (graceful, без 500); 8-поточный тест на реальном Postgres. Дополняет идемпотентность потребителя P1-7. 397 тестов. |
| P2-2 | Парсеры ARINC 620/618 / Type B / AFTN | integration-dev | Done | reviewer PASS (правка: openapi обновлён). Парсеры ARINC 618/620/Type B/AFTN (integration.parser), raw-эндпоинт `/messages/incoming/raw` в integration (вызов eventprocessor через port-in NamedInterface — цикл границ разрешён). Извлечение tail/flight/type/payload/externalMessageId, AN/FI паритет. Тесты на реальных примерах + негатив. Без миграции. 443 теста. |
| P2-3 | Исходящий шлюз: uplink/ground через Outbox | integration-dev + db-dev | Done | reviewer PASS (1 цикл bug-fixer: дедуп outbound при replay). Durable-шлюз: ACTION send → персист OutboundMessage(PENDING) атомарно с переходом → OutboundMessageDeliveryScheduler доставляет с claim single-fire (V26). Идемпотентность при рестарт-replay: дедуп по (instance,step) + partial UNIQUE (V27), optimistic-lock предотвращает гонку. origin/recipients сохранены. 469 тестов. |
| P2-4 | Позывные + таблица `callsign_matching` → определение FI | integration-dev + db-dev | Done | reviewer PASS (правка: openapi обновлён). V28 callsign_matching (icao_carrier_code, flight_number, valid_from/to, days_of_week битмаска ISO, dep/arr airport, specificity). CallsignParser (ICAO/IATA) + CallsignMatchingService (findCandidates+дофильтр день/номер/аэропорт → max specificity, tie-break createdAt). Интеграция в raw-приём: FI заменяется только при матче, AN/явный FI не ломаются. 511 тестов. |
| P2-5 | Источники позиций ACARS/ADS-B/radar, фактические vs оценочные | integration-dev | Done | reviewer PASS. Источники/estimated-исключение/in-last-{x}-min уже были из P1-1 (V21). Добавлен главный пробел — «not reported через Off-таймстамп»: V29 `flight_stage_events` (durable времена OOOI), offTime в контексте, evaluatePosition reported=false → окно max(now-{x}, offTime), Off неизвестен → false (не тривиально true до взлёта). 531 тест. NB: V29 написал integration-dev (не db-dev) — ревью качества пройдено. |
| P2-6 | DLQ + ручной reprocess + ретраи/backoff + circuit breaker | integration-dev + db-dev | Done | reviewer PASS (1 цикл bug-fixer: транзакции адаптеров + simulate-failure jsonb; +доводка openapi/метрики). DLQ dead_letter_messages + reprocess/discard (/dlq/** за RBAC OPERATOR/ADMIN); exp-backoff (next_attempt_at); durable circuit breaker (CLOSED/OPEN/HALF_OPEN, своя реализация без resilience4j). V30. DLQ-метрики Micrometer. 596 тестов. |
| P2-7 | Нагрузочный замер входящего потока (k6/Gatling), отчёт `docs/perf/` | test-engineer | Done | reviewer PASS. k6-скрипты (perf/k6/: steady/ramp/duplicate-storm + lib), отчёт docs/perf/README.md (proposed SLO, методика docker run grafana/k6, числа сверены с results JSON). Индикативный прогон: устойчиво до ~220 msg/s (p95 11-26мс), деградация 220→250, на 400 p99>2с. results/ gitignored. Числа честно помечены как требующие приёмочного прогона (P6). 596 тестов (backend не тронут). |

## P3 — Шаблоны / поля / алерты / уведомления

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P3-1 | Движок шаблонов downlink/uplink/ground, computer-generated/external-user, CRUD + OpenAPI | templates-dev + db-dev | Done | reviewer PASS (1 цикл bug-fixer: missing-variable в outbound). Новый модуль `templates` (границы чисто, integration→templates.port.in). Плейсхолдеры {{var}} single-pass, render строгий / tryRender мягкий. CRUD /api/v1/templates (RBAC ADMIN/OPERATOR), V31 таблица templates. Рендеринг в outbound: not-found→fallback, missing-var→markFailed+метрика (не мусор в канал). OpenAPI обновлён. 655 тестов. |
| P3-2 | Движок custom fields: извлечение, хранение per-flight, подстановка | templates-dev + db-dev | Done | reviewer PASS. Новый модуль `customfields` (синк, границы чисто, 0 циклов между 4 модулями). Правила извлечения CONTENT(regex 1 группа)/METADATA; per-flight хранение (uq aircraft+flight+field); подстановка в шаблоны (merge в params на этапе шага → детерминизм retry, явные params выигрывают); значение доступно критериям (БЕЗ 7-го типа — инвариант 6 типов цел); закрытие на IN/SUMMARY (soft-close). V32. CRUD /custom-field-rules (RBAC). OpenAPI. 717 тестов. |
| P3-3 | Условия/алерты raise/close, уровни No/Low/Medium/High/Critical, авто-закрытие | alerts-dev | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 752 теста, JaCoCo "All coverage checks have been met", ModularityTests зелёный). Новый модуль `conditions` (синк, структурный аналог `customfields`/`templates`, 0 циклов): per-flight `raised_conditions` (aircraft+flight+name), независимый alertLevel No/Low/Medium/High/Critical, "нельзя поднять дважды" = частичный UNIQUE активной строки (V33) + явная проверка → `ConditionAlreadyRaisedException`, мягкое закрытие (closed_at), авто-закрытие на IN/SUMMARY. Рефактор: условия вынесены из `integration` (in-memory per-aircraft Map) в durable per-flight — удалены `execution.port.out.ConditionQueryPort` и `MessageOutputPort#raise/closeCondition`; `ActionStepRule`→`conditions.port.in` напрямую; `ExecutionService` читает активные условия per-flight; `eventprocessor`→`FlightConditionLifecycleUseCase` на IN/SUMMARY. Семантика: re-raise активного → шаг FAILURE, нестандартный alertLevel → FAILURE. Починен баг транзакционного отравления: `@Transactional(noRollbackFor=ConditionAlreadyRaisedException)` — без него re-raise в GOTO-цикле ронял переход движка `UnexpectedRollbackException` (2 теста P1_2). Метрики `eca.conditions.raised/closed/active/rejected`. GET /api/v1/conditions (RBAC OPERATOR/ADMIN). OpenAPI обновлён. 752 теста. |
| P3-4 | Event Handling (folder + sequence override) + Notify-каналы идемпотентно | alerts-dev + db-dev | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 772 теста, JaCoCo "All coverage checks have been met", ModularityTests зелёный). Новый модуль `eventhandling` (чистый потребитель события `execution.event.StepNotificationEvent`, без внешних потребителей своих port.in → @NamedInterface не нужен). V34: `folders` (дерево parent_id) + `sequences.folder_id` (nullable FK, простой Long — границы Modulith целы) + `event_handlers` (scope FOLDER/SEQUENCE, trigger ON_SUCCESS/ON_FAILURE/ON_ANY, channel EMAIL/WEBHOOK, target) + `notification_deliveries` (UNIQUE дедуп `(execution_id, step_index, result, handler_id)`). `EventHandlerResolver`: sequence-level override, иначе nearest-wins наследование вверх по дереву папок. `NotificationDispatchService` (@ApplicationModuleListener) — идемпотентная доставка (закрыт TODO ADR-0002/NotificationEventListener): existsByDedupKey + UNIQUE defense-in-depth. Каналы email/webhook — лог-заглушки транспорта (как ACARS-вывод), с валидацией target. StepNotificationEvent +sequenceId +folderId. assignToFolder в sequence (REST `PUT /sequences/{id}/folder`). CRUD `/api/v1/folders`, `/api/v1/event-handlers` (RBAC OPERATOR/ADMIN). Метрики `eca.notifications.sent/failed/duplicate_skipped`. OpenAPI обновлён. 772 теста. |

## P4 — Безопасность

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P4-1 | RBAC user-rights (роли→права→проверки на эндпоинтах) | security-agent + db-dev | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 782 теста, JaCoCo gate, ModularityTests зелёный). Гранулярные user-rights (`Permission` enum, паритет SITA): VIEW/MANAGE_SEQUENCES, MANAGE_EXECUTIONS, VIEW/MANAGE_TEMPLATES, VIEW/MANAGE_CUSTOM_FIELDS, VIEW_CONDITIONS, MANAGE_EVENT_HANDLING, MANAGE_DLQ, MANAGE_USERS, VIEW_AUDIT_LOG, SYSTEM_ADMIN. Роль раскрывается в права (`Role.getPermissions()`, кодовый маппинг — источник истины; OPERATOR=операционные read+exec/dlq/eventhandling, ADMIN=все). JwtAuthenticationFilter выдаёт ROLE_ + права как authorities (формат JWT не изменён). Все эндпоинты переведены с hasRole/hasAnyRole на hasAuthority('<RIGHT>') — SecurityConfig (path-level read) + 32 @PreAuthorize в 8 контроллерах (method-level write). **Закрыт backlog P0-3**: default-deny `/api/**` → authenticated (раньше цепочка кончалась anyRequest().permitAll() = default-allow). GlobalExceptionHandler: AccessDeniedException → 403 (раньше generic-handler отдавал 500 на method-security отказ). Тесты доступа можно/нельзя (P4_1_RbacScenarioIntTest: operator read OK, operator write/users 403, admin 201/200, unauth 401, default-deny 401). Без миграции (RBAC кодовый). 782 теста. NB: конфигурируемые per-role права в БД — возможный follow-up. |
| P4-2 | JWT доступа + refresh с ротацией, BCrypt; ADR токенов/крипто | security-agent | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 788 тестов, JaCoCo gate, ModularityTests зелёный). Короткоживущий access JWT (15 мин) + opaque refresh (256 бит SecureRandom, 7 дней) с durable-состоянием (V35 `refresh_tokens`, хранится SHA-256-хэш). Ротация: каждый /auth/refresh инвалидирует предъявленный токен и выдаёт новую пару; reuse уже отозванного → 401 + отзыв ВСЕЙ цепочки пользователя (фикс транзакционного отравления: `@Transactional(noRollbackFor=InvalidRefreshTokenException)`). /auth/logout отзывает токен. /refresh и /logout открыты (аутентификация самим refresh-токеном). BCrypt для паролей (был), SHA-256 для refresh (высокоэнтропийный) — обосновано в ADR-0003. InvalidRefreshTokenException обрабатывается в AuthController (intra-module, без cycle root↔user). LoginResponse +refreshToken. Тесты: ротация, reuse→401, logout→401, unknown→401, новый access работает (P4_2_RefreshTokenScenarioIntTest). 788 тестов. |
| P4-3 | Вынос секретов из кода, чистка логов, путь к ГОСТ TLS | security-agent + devops-agent | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 788 тестов, JaCoCo gate, Modulith зелёные — изменения конфиг+доки, Java не тронут). Секреты вынесены в env: `application.yml` (`${DB_URL/DB_USERNAME/DB_PASSWORD}`, `${JWT_SECRET}`, TTL) с dev-фоллбэками (тесты/локалка работают без env, прод обязан переопределить); `docker-compose.yml` через `${VAR:-default}` + JWT_SECRET; `.env.example` (`.env` уже gitignored). Хардненинг: `management.info.env.enabled=false` (actuator уже за RBAC SYSTEM_ADMIN). Аудит логов: значения паролей/токенов/секретов не логируются (только username/userId/role/факты). Доки: `docs/security/secrets-management.md` (модель секретов, прод-guidance, secret-manager/импортозамещение), `docs/security/gost-tls-path.md` (путь к ГОСТ TLS: терминация на сертифицированном reverse-proxy перед app, альтернатива КриптоПро Java CSP, чек-лист внедрения). Без миграции/без Java. 788 тестов. |
| P4-4 | OWASP Dependency-Check + SAST в CI, фейл на High/Critical | security-agent + devops-agent | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS 788 тестов + локальный прогон `mvn -Psecurity-scan spotbugs:check` зелёный). Maven-профиль `security-scan` (вынесен из обычной сборки — сканы медленные): OWASP Dependency-Check (`failBuildOnCVSS=7` → фейл на High/Critical CVE, NVD_API_KEY из CI-secret, owasp-suppressions.xml) + SpotBugs/FindSecBugs (`threshold=High`, фильтр только SECURITY-категория, spotbugs-security-include.xml). Новый CI-job `security-scan` (.github/workflows/ci.yml): dependency-check + spotbugs + Semgrep (p/java, p/owasp-top-ten, p/secrets), кэш NVD, аплоад отчётов. **Найдена и исправлена High-находка**: SpaController unrestricted @RequestMapping (CSRF) → @GetMapping (SPA — только навигация). Medium-находки (6× CRLF-log-injection, info-exposure) репортятся, но не валят гейт (DoD = High/Critical) — трекаются follow-up'ом. 788 тестов. |
| P4-5 | Полный audit_log действий пользователя | security-agent + db-dev | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, 794 теста, JaCoCo gate, Modulith зелёные). Дополнен аудит до полного покрытия «кто/что/когда»: USER_LOGIN_FAILED (неудачный вход — несуществующий/отключённый юзер/неверный пароль, с username+reason в деталях, security-критично), USER_LOGOUT, и проставлен actor (`user_id`) на USER_LOGIN/USER_LOGOUT. **correlationId теперь реально пишется** в audit_log — централизованно в `AuditLogJpaAdapter.save` из `CorrelationContext` (MDC HTTP-запроса), покрывает всех писателей (Auth/User/Sequence) без дублирования (закрыт пробел P0-4: колонка V20 была, но никто не заполнял). Существующее покрытие (USER_LOGIN, CREATE_USER, TOGGLE_USER, CRUD последовательностей с actor userId) сохранено. Чтение — GET /api/v1/audit-log за VIEW_AUDIT_LOG (P4-1). Без миграции (V7/V20 достаточно). Тесты (P4_5_AuditScenarioIntTest): login/logout/fail аудируются, correlationId непустой, who/what/when заполнены, RBAC чтения. 794 теста. |

## P5 — Наблюдаемость и надёжность

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P5-1 | Метрики Micrometer→Prometheus (инстансы, msg/s, ошибки, DLQ, латентность, БД, JVM) | observability-agent | Done | reviewer PASS (inline-гейт: `mvn verify` BUILD SUCCESS, JaCoCo gate, Modulith зелёные; фича проверена на реальном app — `/actuator/prometheus` HTTP 200 со всеми метриками). Добавлен `micrometer-registry-prometheus` → эндпоинт `/actuator/prometheus` (exposure + за RBAC SYSTEM_ADMIN P4-1). Новые бизнес-метрики поверх существующих (conditions/notifications/dlq/outbound): `eca.execution.instances.active` (gauge активных RUNNING/WAITING — countActive), `eca.execution.wait_timeout.fired` (counter сработавших таймаутов, только при успешном claim), `eca.execution.event.duration` (timer латентности обработки, p95/p99 через гистограмму), `eca.messages.parsed`/`eca.messages.parse_errors` (throughput msg/s + ошибки парсинга), `eca.integration.dlq.size` (gauge размера DLQ = NEW). Технические — JVM и пул БД (Hikari) — автопривязка Micrometer. Тест P5_1 (`@AutoConfigureObservability` — @SpringBootTest по умолчанию глушит экспорт метрик). Починен ExecutionServiceTest (mock ExecutionMetrics + реальный Timer). Без миграции. |
| P5-2 | Трассировка OpenTelemetry сквозная с корреляцией борт/рейс/инстанс | observability-agent | Done | reviewer PASS (1 цикл bug-fixer + повторное ревью). micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp (runtime) + opentelemetry-sdk-testing (test); экспорт OTLP в самостоятельный коллектор (импортозамещение), по умолчанию `OTLP_TRACING_ENABLED=false` (трейсинг и span'ы включены, экспорт off в dev/CI/тестах). Проброс trace-контекста через @Async-границу @ApplicationModuleListener: `TracingTaskDecorator` (ContextSnapshotFactory) на applicationTaskExecutor (`ObservabilityAsyncConfig`, ObjectProvider→no-op без observability). Доменные span'ы: `eca.engine.process`/`eca.execution.instance.started` (ExecutionService) с aircraft.id/flight.id/execution.instance.id/sequence.id, `eca.integration.outbound` (OutboundMessageGatewayAdapter) с aircraft.id/outbound.type. Тесты: `P5_2_TracingScenarioIntTest` (сквозной сценарий, единый traceId + атрибуты) + `TracingTaskDecoratorTest` (фокусный: РЕАЛЬНАЯ смена потока через ThreadPoolTaskExecutor, кастомный OtelContextAccessor в ContextRegistry, traceId переезжает через границу). Без миграции. 798 тестов, JaCoCo gate, Modulith зелёные. |
| P5-3 | Health liveness/readiness/startup + дашборды Grafana + SLO как код | observability-agent | Done | reviewer PASS (1 цикл правок по ревью: утечка деталей через probe). K8s-пробы Actuator: `liveness`(livenessState), `readiness`(readinessState+db+integrationChannels), `startup`(readinessState+db); probes.enabled→HTTP 200/503. Доменный `IntegrationChannelsHealthIndicator` (в модуле integration, без кросс-границ): OPEN circuit breaker→DOWN, DLQ(NEW)>порога(app.health.dlq-critical-size=100)→OUT_OF_SERVICE, иначе UP. SecurityConfig: точечный permitAll ТОЛЬКО на /actuator/health/{liveness,readiness,startup} (k8s-пробам JWT недоступен); полный /actuator/health и /actuator/** — за RBAC SYSTEM_ADMIN. **Безопасность (фикс ревью):** `show-details: never` на каждой probe-группе — анонимный probe не раскрывает тип СУБД/имена каналов/состояние breaker; детали только через полный health за RBAC. `observability/` как код: grafana/eca-system-dashboard.json, prometheus/alerts.yml (рост DLQ, падение throughput, ошибки доставки/уведомлений, breaker OPEN), slo.yml (SLO-1..4 с PromQL SLI и error budget), README. Тесты P5_3_HealthProbesIntTest (9): пробы без JWT, liveness независим от каналов, readiness→503 при OPEN breaker/DLQ>порога, негативные (нет утечки деталей анониму), full health за RBAC. Без миграции. 807 тестов, JaCoCo gate, Modulith зелёные. |
| P5-4 | Бэкап/восстановление БД + chaos/failover тесты | db-dev + devops-agent + test-engineer | Done | reviewer PASS (заметки не блокирующие). `ops/backup/`: pg_backup.sh (pg_dump --format=custom --no-owner --no-privileges, retention rotation, env, set -euo pipefail), pg_restore.sh (--exit-on-error), README (логический бэкап + PITR-дизайн: WAL archiving/pg_basebackup/recovery_target_time, RTO/RPO, импортозамещение). `docs/runbooks/chaos-failover.md` (3 сценария + scope-граница). Тесты (пакет reliability, +13 → 820): `P5_4_BackupRestoreIntTest` (Testcontainers, СВОИ контейнеры: SOURCE→Flyway V1-V35→данные VP-BQR/SU1234→pg_dump в контейнере→pg_restore в TARGET→8 проверок целостности SOURCE↔TARGET); `P5_4_ChannelChaosIntTest` (extends BaseIntegrationTest: breaker CLOSED→OPEN→readiness 503→HALF_OPEN→CLOSED→readiness 200, fail-fast attempts=0); `P5_4_DatabaseChaosIntTest` (Testcontainers без Spring: стоп контейнера→getConnection SQLException ~3с<5с HikariCP, health DOWN ~3с, lifecycle UP→DOWN→UP). **Scope-граница:** настоящий replica-failover (репликация/leader election) → P6-1, не реализован (одноузловая БД). Без миграции. mvn verify BUILD SUCCESS, 820 тестов, JaCoCo gate, Modulith зелёные. NB(follow-up): `P5_4_BackupRestoreIntTest` хардкодит ожидание 35 миграций/версию '35' — обновить при добавлении V36 (P6); DB-chaos тайминг 3с<5с — запас 2с, на медленном Windows-CI возможна флейки. |

## P6 — Масштаб

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P6-1 | HA: реплики backend + leader election планировщика (single-fire в кластере) | devops-agent (реализовано оркестратором — суб-агент уперся в лимит сессии) | Done | reviewer PASS (1 цикл фикса: @Autowired на public-конструкторе — два конструктора ломали выбор бина). Lease-based leader election на PostgreSQL (новый модуль `cluster`: `LeaderElection`/`LeaderElectionService`), V36 `leader_election` (lock_name PK, holder_id, lease_until). Захват/продление — атомарный `INSERT ... ON CONFLICT DO UPDATE ... WHERE lease_until<now OR holder=me` (из N реплик лидер ровно один); heartbeat@10s, acquireOnStartup@ApplicationReadyEvent, releaseOnShutdown@PreDestroy. @Scheduled-тики гейтятся `isLeader()` (WaitTimeoutScheduler.pollWaitTimeouts; OutboundMessageDeliveryScheduler.scheduledPoll→pollPendingMessages, последний оставлен public/ungated — его зовут IT-тесты напрямую). DB-claim (P1-5/P2-3) — defense-in-depth, корректность single-fire от лидерства НЕ зависит. Импортозамещение: без ShedLock/Quartz/ZooKeeper, только PostgreSQL (ADR-0004). k8s `deploy/k8s/backend-deployment.yaml` (replicas:2, P5-3-пробы, graceful shutdown server.shutdown=graceful + lifecycle.timeout 25s + preStop). Тест P6_1_LeaderElectionIntTest (5, изолирован своим lock_name): один лидер из двух, renew, failover при протухании, release→перехват, isLeader=false при истёкшей локальной аренде. P5_4_BackupRestoreIntTest future-proofed (сверка TARGET↔SOURCE + floor>=36). 825 тестов, JaCoCo gate, Modulith зелёные. |
| P6-2 | Горизонтальное масштабирование + партиционирование/retention больших таблиц | db-dev | Done | reviewer PASS (1 цикл фикса: добавлены Micrometer-метрики retention). **Партиционирование:** V37 переводит `tracking_event_log` на нативное `PARTITION BY RANGE (created_at)` помесячно (INSERT...SELECT с сохранением id/IDENTITY, DEFAULT-партиция, индексы на родителе, PK стал составным (id, created_at) — JPA `@Id Long id`/findById работают; FK на таблицу нет). `messages`/`audit_log` — НЕ нативно (составной PK сломал бы JPA-критерии/cross-module writers/>10 тестов), вместо этого **retention-by-deletion** по received_at/created_at. **Retention:** `RetentionService` (корневой пакет, JdbcTemplate) — create-ahead будущих партиций + DROP старых + DELETE старых строк; @Scheduled (cron, дефолт 03:00), **гейтуется P6-1 LeaderElection** (чистит только лидер); пороги из `app.retention.*` (env). Метрики `eca.retention.partitions.dropped` / `eca.retention.rows.deleted{table}`. **Масштабирование:** `deploy/k8s/backend-hpa.yaml` (HPA autoscaling/v2, CPU 70%, min 2 / max 8; requests из P6-1 deployment) + README. Тесты P6_2_PartitioningRetentionIntTest (18): роутинг строк по партициям (tableoid), cross-partition чтение, составной PK + findById, create-ahead, DROP старой партиции с данными, retention messages/audit (граница порога), leader-гейт (no-op на не-лидере), метрики. 843 теста, JaCoCo gate, Modulith зелёные. |
| P6-3 | Полный нагрузочный прогон + профилирование + тюнинг, отчёт p95/p99 | test-engineer (дописано оркестратором — суб-агент уперся в лимит) | Done | reviewer PASS (числа сверены с сырьём k6). Приёмочный прогон k6 по уровням 20..400 msg/s (`perf/k6/p6-3-acceptance.js`, отчёт `docs/perf/P6-3-acceptance.md`). **Кривая:** устойчиво до ~220 msg/s (p95≤26мс, p99≤222мс, 0% ошибок); колено ~220→250 (на 250: p95 401мс, p99 1.2с); потолок ~375/s факт; 0% ошибок на ВСЕХ уровнях (graceful деградация латентностью, без потерь); duplicate-storm идемпотентен. **Профиль горячего пути:** низкий p50 (~6-7мс) + взрыв p99 + 0% ошибок = конкуренция за connection (очередь Hikari), не CPU-bound. **Тюнинг:** Hikari maximum-pool-size 10→20 (env DB_POOL_MAX, minimum-idle 5) — устранение очереди за соединением; безопасно по Postgres max_connections (2 реплики×20=40<100). Честно помечено: числа — дев-машина, re-run на тюнинге/прод-железе + серверное профилирование (hikaricp.pending) + async-executor sizing → follow-up. SLO P5-3 выполняются до устойчивого уровня. Без миграции/Java. 843 теста (конфиг не сломал старт), JaCoCo gate, Modulith зелёные. |

## P7 — Промышленный фронтенд (завершена 2026-06-28)

Коммиты: `2d95c2e` P7-1, `a0c2292` P7-2, `5a48130` P7-3, `6e39988` P7-4, `53d9a59` P7-5.

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P7-1 | Каркас FE: слои, стор, API-клиент из OpenAPI, WebSocket-слой; ADR фронтенда | frontend-architect | Done | reviewer PASS (1 цикл фикса: добавлена тест-инфра + staged generated + JWT-TODO). Рефактор-каркас существующего FE (~8200 строк): **стор Zustand 5** (authStore persist `eca-auth` partialize только user; uiStore тема/сайдбар; useAuth — shim, компоненты не тронуты). **API-клиент из OpenAPI**: openapi-typescript, скрипт `gen:api` → `src/api/generated/openapi.ts` (коммитится) + schema.ts (Api*-псевдонимы); образец — templatesApi/dlqApi/conditionsApi на сгенерированных типах (план вытеснения ручных types/ в ADR). **WS-слой** `src/api/ws/` (WsClient singleton: URL из VITE_WS_URL, NO-OP без URL, экспоненциальный reconnect 1..32с, ping/pong, мультиплекс-подписки; useSubscription хук; типизированный WsMessage union). Серверный WS + WS-auth первым сообщением (не JWT-в-URL, TODO+ADR) → P7-4. **Тест-инфра** (новое для FE): vitest 4 + @testing-library/react + jsdom, 21 тест (WsClient no-op/мультиплекс/ping/backoff, useSubscription cleanup, authStore partialize/clear). ESLint 9 flat config (baseline был сломан — без конфига). ADR-0005 (слои, выбор стора, OpenAPI-контракт, WS, i18n-план→P7-5). Гейты: npm check=0, build OK, lint 0 errors/66 warnings (было 68), vitest 21 зелёные. Коммит `2d95c2e`. |
| P7-2 | Редактор React Flow: ноды шагов, рёбра-решения, drag-n-drop GOTO, start/stop-критерии | ui-agent | Done | reviewer PASS (3 цикла фикса i18n + контракт GOTO; полнота вынесена в финальном проходе — grep на кириллицу чист). Редактор последовательностей на React Flow: кастом-ноды 3 типов (ACTION/EVALUATE/WAIT), рёбра-решения отдельно для true/false (CONTINUE/GOTO/END/ABORT + Notify), панель start/stop-критериев. **Ключевое:** `gotoUtils.ts` — чистая логика пересчёта ссылок GOTO при reorder/insert/delete (цель сохраняет смысл), покрыта тестами (граничные: self-loop, out-of-range, удаление цели, цепочки). Editor-стор Zustand (`sequenceEditorStore`). **i18n:** все пользовательские строки в `i18n/dict.ts` (RU/EN-паритет, вкл. вложенные configLabels/configLabelsShort/criterionLabels) через `useEditorI18n` — каркас под P7-5; 0 кириллических литералов в компонентах. Без any. Детальные формы 6 критериев → P7-3, реал-тайм → P7-4, полный i18n-фреймворк → P7-5. Гейты: npm check=0, build OK, lint 0 errors/66 warnings, vitest 45 тестов (было 21, +24). Коммит `a0c2292`. |
| P7-3 | Формы шагов и критериев, шаблоны, алерты, привязка к бортам, валидация | ui-agent | Done | reviewer PASS (1 цикл bug-fixer: 4 блокера — timeoutSeconds на top-level запроса, 2× i18n-хардкод 'GROUP'/'AUTO'/'USER', TODO про aircraft-bindings). **StepFormV2.tsx** — формы 3 типов шагов: ACTION (5 подтипов: RAISE/CLOSE_CONDITION с уровнем алерта, SEND_UPLINK computer-generated\|external-user + шаблон, SEND_GROUND + получатели, WAIT_TIME sec/min/hour); EVALUATE_IF; WAIT_FOR (fromThisPointOnly чекбокс, таймаут). Решения CONTINUE/GOTO/END/ABORT + Notify, отдельно для true/false. **CriteriaBuilder.tsx** — рекурсивный конструктор 6 типов критериев (MESSAGE_RECEIVED, FLIGHT_STAGE, POSITION_REPORTED, TIME_COMPARISON, CONDITION_ACTIVE, COMPOUND AND/OR). **stepFormParsing.ts** — чистые функции parseInitialValues/buildConfigJson: timeoutSeconds из top-level step, не из configJson (паритет бэкенда); 13 тестов. **criteriaValidation.ts** + **stepValidation.ts** — чистая рекурсивная валидация (edge-кейсы: пустые поля, COMPOUND без дочерних, GOTO без step, отрицательные значения). **types/criteria.ts** — CriteriaNode/ActionConfig без any. **i18n dict.ts** ~100 новых ключей (alertLevels, stageOperators, flightStages, timeOperators, timeReferences, positionSources, msgDirections, validationErrors, groupTagLabel, originTagAuto/User) RU/EN паритет, 0 кириллических литералов в JSX. Привязка к бортам — TODO P7-bindings (ждёт backend-эндпоинтов). Гейты: check=0, build OK, lint 0 errors/66 warnings, vitest 183 теста (было 45, +138). Коммит `5a48130`. |
| P7-4 | Реал-тайм статусы инстансов + Event Log через WebSocket + таймлайн | ui-agent | Done | reviewer PASS. Серверная сторона: `EcaWebSocketHandler` (`/ws/eca`), `WsSessionRegistry`, `EcaWsBroadcaster` (трансляция `StepTransitionEvent`/`ExecutionStartedEvent`/`ExecutionCompletedEvent`), `TokenValidatorPort`/`JwtTokenValidatorAdapter` (JWT-аутентификация при подключении). Клиентская сторона: `InstancesDashboard` (список активных инстансов + статусы), `EventLogPanel` (поток событий), `ConnectionStatus` (индикатор WS), `instancesStore` (Zustand). Тесты backend — `EcaWebSocketHandlerTest`, `EcaWsBroadcasterTest`; frontend — `instancesStore.test.ts`. Коммит `6e39988`. |
| P7-5 | i18n RU/EN, роль-зависимый UI, a11y, скелетоны/анимации | frontend-architect + ui-agent | Done | reviewer PASS. Полный i18n RU/EN по всем компонентам (миграция строк в `i18n/dict.ts`, 0 хардкода). Роль-зависимый UI: скрытие/блокировка элементов управления, недоступных по RBAC (OPERATOR vs ADMIN). A11y-фиксы: ARIA-атрибуты, keyboard navigation, цветовой контраст. Скелетоны загрузки для асинхронных данных. Коммит `53d9a59`. |

## P8 — Упаковка, деплой, доки, приёмка (завершена 2026-06-28)

Коммиты: `4fcd00a` P8-1, `a48c6b2` P8-2, `6cf206f` P8-3, P8-4 — текущий прогон (без нового коммита кода).

| ID | Описание | Ответственный агент | Статус | Доказательство |
|---|---|---|---|---|
| P8-1 | Kubernetes/Helm чарты, пробы, ConfigMap/Secret, rolling/blue-green, Flyway при деплое | devops-agent | Done | reviewer PASS. Helm-чарт `deploy/helm/eca-system`: backend (Deployment + Service + HPA autoscaling/v2 CPU 70% min2/max8), frontend (Deployment nginx multi-stage + Service), PostgreSQL (StatefulSet + PVC + Service), Ingress, ConfigMap, Secret, `_helpers.tpl`. Профили `values-staging.yaml` / `values-prod.yaml`. Flyway запускается при старте backend-контейнера (встроен в Spring Boot). Frontend упакован в multi-stage Docker: `node:20-alpine` (сборка vite) → `nginx:1.27-alpine` (раздача). Коммит `4fcd00a`. |
| P8-2 | Руководство администратора (RU) + ранбуки эксплуатации + README | docs-agent | Done | reviewer PASS. `docs/admin/`: `installation.md` (требования, docker-compose, Helm-деплой), `configuration.md` (env-переменные, пул БД, JVM, retention), `operations.md` (мониторинг, масштабирование, обновление). Ранбуки `docs/admin/runbooks/`: `service-restart.md`, `db-backup-restore.md`, `incident-high-cpu.md`, `incident-db-connection.md`, `leader-election-stuck.md` (5 сценариев). Коммит `a48c6b2`. |
| P8-3 | Матрица паритета SITA→наше→UAT + license report + материалы для Реестра российского ПО | compliance-agent | Done | reviewer PASS. `docs/compliance/parity-matrix.md` (90 позиций, 86/90 РЕАЛИЗОВАНО на момент создания), `docs/compliance/license-report.md` (анализ лицензий зависимостей, совместимость с Реестром РФ), `docs/compliance/russian-software-registry.md` (материалы для включения в Реестр российского ПО), `docs/compliance/uat-checklist.md` (чеклист UAT для заказчика). Коммит `6cf206f`. |
| P8-4 | Приёмочный прогон по матрице паритета + финальный отчёт готовности | tech-lead + compliance-agent | Done | Финальный прогон 2026-06-28. Верификация всех ЧАСТИЧНО-пунктов матрицы против кода: выявлена ошибка compliance-agent по 15.3 (HPA) — статус исправлен ЧАСТИЧНО→РЕАЛИЗОВАНО (Helm-чарт с HPA реализован в P8-1, `deploy/helm/eca-system/templates/backend/hpa.yaml`). Итог матрицы: **87/90 (97%) РЕАЛИЗОВАНО**, 3 ЧАСТИЧНО (6.3 фильтр ВС, 12.2 radar-feed, 12.3 ADS-B-feed — согласованы с заказчиком). Обновлены: `docs/compliance/parity-matrix.md` (v1.1), `ROADMAP.md` (P7/P8 DONE, раздел «Итог P7–P8»), `docs/PROGRESS.md`. Система готова к UAT. |

---

## Сводные метрики на момент закрытия P8

- Backend тестов: 843 (JaCoCo gate пройден, baseline P6-3; P7-4 backend WS тесты добавлены в рабочую ветку, точный итог после `mvn verify`).
- Frontend тестов: 183+ (vitest, baseline P7-3; P7-4 добавил `instancesStore.test.ts` и WS-тесты).
- Последняя миграция: V37 (`partition_tracking_event_log`, P6-2). P7, P8 — без новых миграций.
- Фазы P0–P8 завершены. Проект готов к передаче заказчику (UAT).
- Матрица паритета: 87/90 (97%), 3 открытых риска согласованы.
- Helm-чарт: `deploy/helm/eca-system` (backend HPA min2/max8, frontend nginx, PostgreSQL StatefulSet).
- Документация: `docs/admin/` (RU, 5 ранбуков), `docs/compliance/` (4 документа), `docs/security/` (threat model, FSTEC, GOST TLS, secrets).

### CI зелёный (2026-06-26): доведён до зелёного на всех джобах
- После починки CRLF в mvnw (28c1170) OWASP-скан впервые реально запустился и валил
  `security-scan`. Починено: OSS Index analyzer (HTTP 401 без токенов) отключён
  `ossindexAnalyzerEnabled=false`; DOMPurify XSS в swagger-ui-5.18.2 (RetireJS) —
  точечные suppressions (Swagger UI за RBAC SYSTEM_ADMIN), коммит 22dc4fd.
- Semgrep (воспроизведён локально через docker semgrep/semgrep) валил 3 блокирующими
  находками: bcrypt-hash дефолтного admin в V8 (необратим = не секрет, V8 — применённая
  миграция, править нельзя) → точечный `.semgrepignore`; 2× dockerfile missing-user-entrypoint
  → non-root USER в backend/Dockerfile и корневом Dockerfile. Коммит 53103f8.
- **Гейты воспроизводятся локально (репо приватное, gh/токена нет):** backend `mvn verify`,
  OWASP dependency-check, SpotBugs, Semgrep — все зелёные.

### CI-регрессия (2026-06-24): красный пайплайн — починено
- **Корень обеих красных джоб (Backend + Security scan): `mvnw` хранился в git с
  CRLF-окончаниями.** На Linux-раннере `bash mvnw` падал сразу: `mvnw: line 37:
  syntax error near unexpected token $'in\r'` → exit 2. OWASP-скан при этом ни разу
  не выполнялся (CVE не находились — степ умирал до запуска). Гипотезы «нет postgres»
  и «уязвимые зависимости» не подтвердились по логам CI (postgres стартовал, отчёт
  dependency-check не генерировался вовсе).
- **Фикс:** нормализация `mvnw` → LF + `.gitattributes` (`mvnw`/`*.sh` = LF,
  `*.cmd`/`*.bat` = CRLF), чтобы не регрессировало. Превентивно: Spring Boot
  3.5.0 → 3.5.15 (свежие патчи транзитива; локально `mvn verify` зелёный, 796 тестов).

## Backlog / follow-up (отложенные, зафиксированы при ревью)

- ~~**SpotBugs/FindSecBugs Medium-находки (из P4-4)**: 6× CRLF_INJECTION_LOGS + 1× INFORMATION_EXPOSURE~~ — **ЗАКРЫТО Фазой 3 прогона 2026-07-03**: реальная внешняя атак-поверхность санитизирована (`LogSanitizer`: MessageController, AuthController, UserService); INFORMATION_EXPOSURE в DLQ — intentional-by-design (за RBAC MANAGE_DLQ). Остаточные внутренние Medium-находки — осознанное решение по объёму, см. BLOCKERS.md.
- ~~**Default-deny в SecurityConfig** (из P0-3 ревью)~~ — **ЗАКРЫТО в P4-1**: `/api/**` → authenticated (default-deny), задокументировано в строке P4-1.
- ~~**Dedup startExecution — unique constraint / claim-механизм** (из P1-7 ревью)~~ — **ЗАКРЫТО Фазой 1 прогона 2026-07-03**: V38 частичный UNIQUE-индекс + идемпотентный no-op (ADR-0007).
- ~~**CLAUDE.md канонические метрики устарели**~~ — **ЗАКРЫТО Фазой 0 прогона 2026-07-03** (санкция Дениса): CLAUDE.md актуализирован.
- ~~**Гейтинг @Scheduled-поллеров по ApplicationReadyEvent** (из P2-3 ревью)~~ — **ЗАКРЫТО Фазами 1–2 прогона 2026-07-03**: `ApplicationReadiness`-гейт (Ф1) + структурное отключение планирования в тестах `SchedulingConfig`/`app.scheduling.enabled` (Ф2).
- **Процесс: миграции пишет db-dev** (из P2-5): V29 написал integration-dev сам (ревью качества пройдено, дефектов нет), но по TEAM.md/CLAUDE.md схему ведёт db-dev. Впредь миграции — через db-dev (как V25/V26/V27/V28). Замечание процессное, не технический долг.
- ~~**Мёртвый код-риск** (из P3-2 ревью): `CriterionEvaluator.getCustomFieldValue`~~ — **ЗАКРЫТО Фазой 1 прогона 2026-07-03**: метод удалён, тесты переориентированы на `getActiveValues`.
- ~~**Посторонний git worktree** `.claude/worktrees/strange-jang-f38717`~~ — **ЗАКРЫТО Фазой 8 прогона 2026-07-04**: worktree снят (изменения в нём — чистый CRLF-шум), ветка с 18 уникальными коммитами сохранена — решение об удалении за Денисом (BLOCKERS.md).
- JaCoCo gate baseline: LINE ≥ 0.88, INSTR ≥ 0.90 (цель проекта — 85% по
  изменённому коду на гейте ревью, см. CLAUDE.md, п.5 рабочего протокола).
- `ApplicationModules.verify()`: зелёный, нарушений границ не найдено.
- Последний коммит фундамента: `42aabec` ("P0 guardrails: CI/CD, OpenAPI,
  structured logging + audit_log").
- **P7 aircraft-bindings (TODO из P7-3)**: привязка к бортам через UI ждёт backend-эндпоинтов списка бортов; при необходимости — отдельный тикет после UAT. **Закрыто Фазами 5–6 прогона 2026-07-03** (GET /api/v1/aircraft + AircraftPicker).
- **LOW-заметки ревью Фазы 6 (2026-07-04, не блокирующие)**: (1) `MessageLog.tsx` без собственного компонентного теста — интеграция AircraftPicker покрыта только E2E `aircraft-filter.spec.ts`; (2) `AircraftPicker` при внешне заданном `value` вне загруженных options показывает «сырой» tail number без обогащённого лейбла до первого fetch — при необходимости персистентного выбора рассмотреть `labelInValue`.

*Обновлено: 2026-06-28 (P8-4, финальный приёмочный прогон — все фазы P0–P8 завершены).*


---

# Прогон «Промышленный апгрейд» (2026-07-03)

## Фаза 0 — Аудит, синхронизация фактов, план ✅
- **Незакоммиченные файлы разобраны** (коммит `bf07d31`, до старта фаз): `.gitignore` (+`ecc-source`), `docker-compose.yml` (порт хоста 8081→8080, postgres без публикации 5432), `instancesStore.test.ts` (фикс кавычек) — осмысленные, закоммичены.
- **AUDIT.md** создан: полный аудит по 12-пунктному чек-листу со ссылками на файлы-доказательства; статусы СДЕЛАНО/ЧАСТИЧНО/ПРОБЕЛ.
- **IMPROVEMENT_PLAN.md** создан: сверка backlog `docs/PROGRESS.md` с кодом, приоритеты P1/P2/P3, привязка к фазам 1–8.
- **CLAUDE.md** актуализирован (санкция Дениса): 23 таблицы, V1–V37, ~843 backend + 183+ frontend тестов, JaCoCo LINE≥0.88/INSTR≥0.90, порт 8081, 10 модулей backend.
- DoD: рабочее дерево чистое после коммита; CLAUDE.md актуален. Docs-only фаза — verification loop не затрагивается (код не менялся).

## Фаза 1 — Целостность движка под HA ✅
- **Dedup startExecution (P1-7/P6-1)** — миграция **V38** `idx_exec_dedup_trigger_unique`: частичный УНИКАЛЬНЫЙ индекс `(sequence_id, aircraft_id, flight_number, triggering_message_id)` `NULLS NOT DISTINCT WHERE triggering_message_id IS NOT NULL` (заменил неуникальный `idx_exec_dedup_trigger` V23). Гарантия «ровно один старт» перенесена на уровень БД (как single-fire WAIT-таймаутов P1-5). `ExecutionService`: event-driven старт изолирован в `REQUIRES_NEW` (`startExecutionInNewTransaction`), проигрыш гонки ловится грациозно как идемпотентный no-op (`startExecutionDeduplicated`) + метрика `eca.execution.start.duplicate_rejected`. TDD: новый `P1_9_ConcurrentStartDedupScenarioIntTest` (8 потоков -> ровно 1 инстанс; детерминированные тесты ограничения БД, NULLS NOT DISTINCT, частичного WHERE). Индекс подтверждён срабатывающим под конкуренцией (SQLState 23505).
- **Гейтинг @Scheduled по готовности (P2-3)** — новый публичный API модуля `cluster`: `ApplicationReadiness` + `ApplicationReadinessGate` (взводится на `ApplicationReadyEvent`). Гейтят `WaitTimeoutScheduler`, `OutboundMessageDeliveryScheduler`, `RetentionService`; `LeaderElectionService.heartbeat` — self-contained ready-флаг (лидерство впервые захватывается только в `acquireOnStartup`). Границы Modulith целы (`ApplicationModules.verify` зелёный).
- **Мёртвый код (P3-2)** — удалён `CriterionEvaluator.getCustomFieldValue` (production-вызовов нет); тесты `CriterionEvaluatorTest`/`P3_2` переориентированы на проверку доступности значения через `getActiveValues`.
- Обновлён `V23TriggeringMessageIdMigrationIntTest` под V38 (проверяет финальное состояние схемы — уникальный индекс, старый удалён).
- Наблюдаемость нового пути (метрика dedup) и панели/SLO — Фаза 4 (по плану прогона).
- **Ревью-гейт (1 цикл CHANGES REQUESTED → добивка тестов):** `reviewer` нашёл HIGH — не покрыта «отрицательная» ветка гейтинга `ApplicationReadiness.isReady()==false` ни для одного из 4 поллеров; MEDIUM — метрика `eca.execution.start.duplicate_rejected` без ассерта. Добавлены unit-тесты (TDD-добивка, 10 тестов): `WaitTimeoutSchedulerTest` (not-ready/not-leader/ready+leader), `RetentionServiceTest` (not-ready/not-leader → БД не трогается), `LeaderElectionServiceTest` (heartbeat до `ApplicationReadyEvent` = no-op), `OutboundMessageDeliverySchedulerTest#SchedulingGate` (гейты на `scheduledPoll`), `ExecutionMetricsTest` (детерминированный инкремент dedup-счётчика). Повторный проход зелёный.
- DoD: конкурентный тест двойного старта зелёный; mvn verify зелёный (871+10 тестов), JaCoCo LINE>=0.88/INSTR>=0.90 держится; reviewer PASS (после добивки). Коммит `ca42516`.

## Фаза 2 — Стабилизация и добивка тестов backend ✅
- **Покрытие изменённых фазой 1 классов** — все выше 85% line (по JaCoCo-отчёту): `ApplicationReadinessGate` 100%, `ExecutionMetrics` 100%, `WaitTimeoutScheduler` 100%, `OutboundMessageDeliveryScheduler` 96%, `ExecutionService` 96%, `LeaderElectionService` 93%, `CriterionEvaluator` 93%, `RetentionService` 89%. Добивка не требовалась.
- **Найдена и устранена реальная флейки (подвисание прогона)** — второй полный `mvn verify` подвис на ~28 мин на `P3_2_CustomFieldsEngineScenarioIntTest`. Диагностика через `pg_stat_activity`: **взаимная блокировка авто-тика `@Scheduled`-поллера и межтестового Flyway-clean** — фоновый `OutboundMessageDeliveryScheduler.scheduledPoll` успел открыть транзакцию и взять блокировку (`UPDATE outbound_messages SET status='SENT'`), а Flyway `DROP TABLE outbound_messages CASCADE` (изоляция между тест-классами) встал в ожидание этой блокировки без таймаута. Первый прогон случайно успел закоммитить до clean — отсюда недетерминизм. Пред-проверка `ApplicationReadiness` (фаза 1) закрывает только СТАРТ (в проде Flyway `migrate` проходит до открытия ready-гейта), но не повторяющийся межтестовый `clean` (которого в проде нет).
- **Фикс (корень):** `@EnableScheduling` вынесено из `EcaApplication` в отдельный `SchedulingConfig`, гейтированный `@ConditionalOnProperty(app.scheduling.enabled, matchIfMissing=true)`. В тестах surefire (`systemPropertyVariables`) задаёт `app.scheduling.enabled=false` — авто-тик поллеров выключен во всех форках, гонка с Flyway-clean устранена в корне; тесты и так вызывают методы опроса напрямую (ungated `pollPendingMessages`/`checkWaitTimeouts`), покрытие логики не страдает. Прод: свойство не задано → планирование включено штатно. Тест `SchedulingConfigTest` (ApplicationContextRunner: true/false/missing). **Подтверждение фикса:** полный прогон прошёл ТУ ЖЕ точку P3-2 без подвисания; из 884 тестов упал ровно 1 — сам `SchedulingConfigTest` (эффект JVM-wide `-D`, поправлен `withSystemProperties`), т.е. НИ ОДИН тест не зависел от авто-тика.
- DoD: mvn verify зелёный; гейт LINE ≥ 0.88 / INSTR ≥ 0.90 держится; недетерминированное подвисание устранено структурно (поллеры физически не тикают в тестах). Коммит `0d58110`.

## Фаза 3 — Безопасность: rate limiting + санитизация логов ✅
- **Rate limiting (единственный незакрытый пункт чек-листа ИБ)** — bucket4j (`com.bucket4j:bucket4j_jdk17-core` 8.19.0, Apache-2.0, совместимо с Реестром РФ; координаты/лицензия/транзитивы сверены с Maven Central — 0 транзитивных зависимостей). `RateLimitFilter` (token bucket, per client IP с учётом X-Forwarded-For): `AUTH` — `/api/v1/auth/login|refresh|logout` (брутфорс, дефолт 10/60c; `/me` и `/register` исключены), `MESSAGES` — открытый ингест `/api/v1/messages/**` (флуд, дефолт-потолок высокий 2000/s — выше пика ~375 msg/s, штатный поток шлюза не режется). Отказ — 429 в формате RFC 7807 Problem Details + `Retry-After`; метрика `eca.ratelimit.rejected{scope}` (Micrometer). Добавлен в security-цепочку ПЕРЕД `JwtAuthenticationFilter` (после CorsFilter — 429 сохраняет CORS-заголовки), двойная servlet-регистрация подавлена `FilterRegistrationBean.setEnabled(false)`. Пороги/тумблер — `app.ratelimit.*` (env, по профилям); в тестах surefire задаёт `app.ratelimit.enabled=false` (пассивный passthrough — не режет высоконагруженные IT ингеста/логина). TDD: `RateLimitFilterTest` (8 тестов: лимит/429/Problem Details/Retry-After/метрика, независимость IP, отдельные scope, нелимитируемые пути, `/me`, OPTIONS, disabled-passthrough, XFF-ключ).
- **CORS вынесен из кода** (пробел аудита фазы 0): захардкоженные dev-origins в `SecurityConfig` → `app.cors.allowed-origins` (env `APP_CORS_ALLOWED_ORIGINS`, список через запятую; dev-дефолт vite/CRA); DSL переведён на `Customizer.withDefaults()` (бин `corsConfigurationSource`).
- **Санитизация логов (backlog P4-4 / CRLF_INJECTION_LOGS)** — утилита `LogSanitizer` (заменяет CR/LF/C0/C1 на `_`), тест `LogSanitizerTest` (6). Применена к **реальной внешней атак-поверхности**: открытый ингест (`MessageController`: aircraft/template/type), открытый логин (`AuthController`: username на login/failed-login/register), `UserService` (username). **Важно про объём:** SpotBugs при threshold=Low даёт **138** CRLF-находок в ~40 классах, НО все Medium — на гейте CI (threshold=High) их **0** (сборка чистая). Большинство — внутренние ID/enum (не форжатся злоумышленником), а FindSecBugs не распознаёт кастомный cleanser (находка остаётся и после sanitize). Тотальная санитизация 138 строк — диспропорциональна и технически неэффективна → зафиксировано в `BLOCKERS.md` как осознанное решение по объёму + follow-up.
- **SpotBugs Medium false-positives (разобраны):** `SQL_INJECTION_SPRING_JDBC` в `RetentionService` — динамический DDL партиций (идентификатор таблицы нельзя параметризовать), имя из `YearMonth`/regex-запроса pg_catalog, внешнего ввода нет → добавлен defense-in-depth whitelist-guard `assertSafePartitionName` (`^tracking_event_log_\d{4}_\d{2}$`) перед CREATE/DROP. `INFORMATION_EXPOSURE` в `DeadLetterQueueService.stackTraceOf` — стектрейс для диагностики оператором за RBAC `MANAGE_DLQ`, не анонимная утечка → intentional. Оба Medium (гейт High не валят), задокументированы в `BLOCKERS.md`.
- Наблюдаемость нового пути (метрика 429, dedup) и панели/SLO — Фаза 4 (по плану прогона).
- **Ревью-гейт (1 цикл CHANGES REQUESTED → исправления):** `reviewer` нашёл **HIGH** — `RateLimitFilter.clientIp` безусловно доверял `X-Forwarded-For` (клиент мог спуфить XFF и обойти лимит на прямой топологии docker-compose; + неограниченный рост карт бакетов = memory-DoS). **Исправлено:** XFF учитывается ТОЛЬКО от доверенных прокси (`app.ratelimit.trusted-proxies`, IP/CIDR через `IpAddressMatcher`; дефолт пуст → ключ = `remoteAddr`, безопасно для dev), берётся правый непроверенный хоп; карты бакетов — ограниченный LRU (`maxTrackedKeys`, дефолт 100k). Тест `ignoresSpoofedXForwardedForFromUntrustedSource` (спуфинг разных XFF с одного remoteAddr → один бакет → лимит держится) + обновлён trusted-XFF тест. MEDIUM: (1) синхронизированы `docs/security/threat-model.md` (R-4 закрыт, D-o-S строки → Mitigated) и `fstec-checklist.md` (ИАФ rate limiting → реализован); (2) `@ApiResponse(429)` на `/messages/incoming`, `/flights/stage-change`, `/auth/login|refresh|logout` (регенерация статического `openapi.json` — Фаза 5 по плану); (3) негативный тест `assertSafePartitionName` (SQL-инъекция/битый идентификатор → `IllegalArgumentException`). Повторный проход зелёный (RateLimitFilterTest 9, RetentionServiceTest 4).
- DoD: rate limiting под тестами (9, вкл. anti-spoofing); реальная атак-поверхность логов санитизирована; SpotBugs security-гейт (High) чистый; Semgrep 0 находок; `mvn verify` зелёный, JaCoCo держится; reviewer PASS (после исправлений). Коммит `bf527cf`.

## Фаза 4 — Наблюдаемость: gap-fill новых путей ✅
- **Метрики новых путей уже введены вместе с фичами** (CLAUDE.md §5): `eca.execution.start.duplicate_rejected` (Фаза 1, dedup V38 — регистрируется эагерно в `ExecutionMetrics`), `eca.ratelimit.rejected{scope}` (Фаза 3 — эагерно в `RateLimitFilter`, scope=auth|messages). Структурные логи нового пути тоже на месте (`RateLimitFilter` WARN при 429 с санитизированным IP; `ExecutionService` INFO при идемпотентном no-op дедупа).
- **Prometheus alert-правила** (`observability/prometheus/alerts.yml`, новая группа `eca.security`): `EcaAuthRateLimitSpike` (>0.1/s 429 на /auth = вероятный брутфорс), `EcaMessagesRateLimitSpike` (>1/s 429 на ингесте = флуд/недооценённый потолок), `EcaDuplicateStartRejectedSpike` (>0.05/s дублей старта = аномалия ретрансляции под HA).
- **Grafana** (`observability/grafana/eca-system-dashboard.json`): новый ряд «Безопасность / надёжность» + 2 timeseries-панели (429 по scope; отклонённые дубли старта). Точечная текстовая вставка (LF сохранён) — 103 строки, без переформатирования файла.
- **Тестовый scrape**: `P5_1_MetricsScenarioIntTest` дополнен ассертами наличия `eca_execution_start_duplicate_rejected_total` и `eca_ratelimit_rejected_total` в `/actuator/prometheus` (обе видны при значении 0 — эагерная регистрация). Тест зелёный (2/2).
- **`/actuator/**` за `SYSTEM_ADMIN`, health-пробы открыты** — без изменений (проверено в `SecurityConfig`).
- DoD: новые метрики видны в тестовом scrape (P5_1 зелёный); дашборды/алерты обновлены как код; observability README актуализирован. Config+docs+1 тест-ассерт (прод-Java не тронут — полный verify не требуется, ср. docs-only Фаза 0). Коммит `8317de3`.

## Фаза 5 — API и БД: aircraft-эндпоинты + ревью индексов ✅
- **Aircraft list API (закрытие TODO P7-3 aircraft-bindings)** — `GET /api/v1/aircraft` за RBAC `VIEW_SEQUENCES` (path-level + `@PreAuthorize`). **Ключевая находка:** отдельной таблицы-реестра бортов и данных о ТИПЕ ВС в системе НЕТ — борта существуют неявно как различные `aircraft_id` (tail numbers) в журнале `messages`. Поэтому эндпоинт — проекция `GROUP BY aircraft_id` над `messages`: tail number + последний контакт + число сообщений/рейсов (именно tail number нужен UI для привязки; «фильтр по типу ВС» не реализуем — нет данных, вместо него — подстрочный поиск по tail number). Вертикальный срез без миграции: DTO-record `AircraftSummaryResponse` (цель JPQL-конструктора и тело ответа, DTO≠Entity), 2 `@Query` (с/без поиска, явный `countQuery`=число бортов, `ORDER BY MAX(received_at)`), порт+адаптер, `MessageQueryService.findAircraft`, `AircraftController` (пагинация, clamp size≤200). Тест `AircraftControllerIntTest` (5): 401 без JWT, агрегаты, поиск case-insensitive, пагинация, пустой список.
- **Ревью индексов (EXPLAIN на сиданных 5k сообщений)** — горячие пути УЖЕ покрыты композитными индексами из прошлых фаз: `idx_exec_status_timeout` (status, wait_timeout_at) + `idx_exec_status_updated_at` (WAIT-таймаут/активные инстансы), `idx_exec_aircraft_status`, V38 unique dedup, `idx_outbound_messages_status_next_attempt_at` (outbound-поллер), tracking/messages индексы. **V39 не нужен.** Aircraft-проекция — Seq Scan+GroupAggregate по своей природе (вывод — кардинальность флота, индекс не устраняет полный агрегат); приемлемо для UI-использования. Follow-up для экстремального масштаба (миллионы сообщений): materialized last-seen-таблица / pg_trgm-индекс для поиска — зафиксировано.
- **OpenAPI + фронт-клиент синхронизированы** — `docs/openapi/openapi.json` был устаревшим (2026-06-23, до множества эндпоинтов); перегенерирован из live `/v3/api-docs` (теперь включает `/api/v1/aircraft` и `@ApiResponse(429)` из Фазы 3). Фронт-клиент `frontend/src/api/generated/openapi.ts` перегенерирован (`npm run gen:api`); `npm run check` (tsc) зелёный — регенерация не сломала FE.
- DoD: aircraft-эндпоинт под интеграционными тестами (5); OpenAPI и сгенерированный клиент синхронны; ревью индексов без CRITICAL/HIGH (горячие пути покрыты, V39 не нужен); `mvn verify` зелёный (906). Коммит `5b962d9`, reviewer PASS.

## Фаза 6 — Frontend: aircraft-bindings UI + Playwright E2E ✅
- **Aircraft-bindings UI** — `aircraftApi` (строго типизирован из OpenAPI-контракта `generated/schema`, псевдонимы `ApiAircraftSummaryResponse`/`ApiPageAircraftSummaryResponse`), компонент `AircraftPicker` (AntD Select, серверный поиск с debounce 300ms + race-guard по reqId, i18n RU/EN, a11y `aria-label`, состояние «Поиск бортов…» в notFoundContent при загрузке). Выбор именно tail number (AN) для привязки — раз отдельного реестра/типа ВС нет (Фаза 5). i18n-ключи добавлены в оба словаря `EDITOR_RU`/`EDITOR_EN`. Тесты: `aircraftApi.test.ts` (4 — параметры/trim search/пагинация), `AircraftPicker.test.tsx` (6 — fetch-on-mount, a11y combobox, debounce 300ms детерминированно на fake timers, очистка таймера на unmount, race-guard: устаревший ответ отбрасывается, error-состояние; ручной `cleanup` — в проекте `globals=false`).
- **Первое применение в продукте**: фильтр «по борту» в журнале сообщений (`MessageLog`) переведён со свободного текстового `Input` на `AircraftPicker` — фильтрация по реально известным системе tail numbers из `GET /api/v1/aircraft`.
- **Playwright smoke E2E** (`frontend/e2e/`, `playwright.config.ts`): 6 сценариев зелёные локально против реального стека (backend `spring-boot:run` :8080 ↔ vite dev :5173 через прокси, демо admin/admin): **auth** (логин→редирект+JWT в localStorage; неверный пароль→остаёмся на /login), **sequence-editor** (список открывается; создание последовательности через API тем же токеном → открытие редактора React Flow, канва `.react-flow` видима), **instances-dashboard** (дашборд `/monitoring` + индикатор WS-подключения `WS подключён/отключён`), **aircraft-filter** (сквозной: ингест ACARS-сообщения с уникальным tail → борт появляется в AircraftPicker через серверный поиск → выбор фильтрует журнал). Конфиг: retries 1 (CI 2), trace on-first-retry, screenshot/video при падении, webServer поднимает vite сам. Артефакты gitignored. `@playwright/test` + chromium установлены.
- **Полный FE verification loop зелёный:** `npm run check` (tsc, вкл. e2e) = 0, `npm run lint` = 0 errors/66 warnings (baseline), `npm run test:run` = 208 vitest-тестов (10 файлов; e2e исключён — vitest scoped `src/**`), `npm run build` OK.
- Backend-эндпоинтов хранения привязок в системе НЕТ (привязка выражается через start-criteria, SITA-модель) — `AircraftPicker` даёт переиспользуемый выбор известных бортов для конструирования критериев; отдельная persist-модель привязок не вводилась (вне объёма, не в схеме).
- **NB (env-флейк, не дефект кода):** один прогон полного E2E-сьюта 2026-07-04 массово упал — POST /auth/login «зависал» в браузере при холодном старте vite Playwright'ом, при этом direct/проксированный curl и повторные прогоны (auth и полный сьют) зелёные, `eca_ratelimit_rejected_total{scope="auth"}=0` (rate limiting исключён по метрике живого JVM). Разовое окружение (cold-start contention на дев-машине); при повторении — смотреть трейс `webServer`/бэкенда в момент зависания.
- DoD: aircraft-bindings под тестами (10 vitest, вкл. debounce/race-guard/error); ≥3 E2E-сценария зелёные локально (6, вкл. сквозной aircraft-filter); без `any`, без хардкода строк (i18n RU/EN); FE loop зелёный. Коммит `706edcf`, reviewer PASS (2 LOW — в backlog).

## Фаза 7 — CI/CD: frontend и E2E становятся гейтами, docker build ✅
- **Job `frontend` — теперь гейт**: снят `continue-on-error: true`, добавлены шаги `npm run lint` и `npm run test:run` (до этого 200+ vitest-тестов в CI не гонялись вообще; lint тоже). Порядок: install → typecheck → lint → vitest → build.
- **Новый job `e2e`** (timeout 30 мин): postgres:16-alpine (service, `eca_test`, тот же контракт, что у backend-job) → `bash mvnw -B -DskipTests package` (тесты — гейт job `backend`, не дублируются) → `nohup java -jar target/eca-system-*.jar` c `SPRING_DATASOURCE_*` env (Flyway мигрирует чистую БД, демо admin/admin из V8) → wait-loop на `/actuator/health/readiness` (permitAll P5-3, включает БД+каналы; 120с, при фейле — хвост лога в вывод) → `npm ci` + `npx playwright install --with-deps chromium` → `npx playwright test` (6 сценариев; в CI ретраи ×2, html-репортер — из `playwright.config.ts`). Артефакты при падении: `test-results/` + `playwright-report/` (трейсы/скриншоты/видео) + `backend-e2e.log`. Глоб jar'а — явный `eca-system-*.jar` (локальный `-Psecurity-scan` оставляет посторонний `findsecbugs-plugin-*.jar` в target).
- **Новый job `docker`**: `docker build` backend- и frontend-образов (multi-stage + non-root уже из P8-1 — не дублировались, теги `github.sha`) + `helm lint deploy/helm/eca-system` тремя прогонами (default / `values-staging` / `values-prod`). **Build-only**: секрет registry не задан — push осознанно отсутствует (блокер в BLOCKERS.md с планом разблокировки; WARN'ы lint про missing required values — штатная работа `required`-guard'ов секретов, передаваемых через `--set`).
- **helm lint немедленно окупился — найден реальный дефект чарта P8-1**: `templates/secret.yaml` не парсился вовсе (`function "fullname" not defined`) — шаблонные выражения `{{ fullname }}` в YAML-**комментариях** Go-шаблонизатор Helm всё равно парсит. Чарт с момента создания (P8-1, ревью прошёл) не рендерился ни одним `helm install/lint` — лишнее подтверждение, что гейт нужен. Исправлено (плейсхолдеры без фигурных скобок + предостерегающий комментарий), все 3 lint-прогона зелёные.
- **Локальная репродукция каждого шага (репо приватное, gh нет)**: frontend check/lint/vitest(208)/build — зелёные; Playwright 6/6 против живого стека; `mvnw -B -DskipTests package` OK; **boot-тест jar как в CI** — с CI-идентичными `SPRING_DATASOURCE_*` на :8085: readiness UP через ~6с, `POST /auth/login` admin/admin → 200 (подтверждён весь путь e2e-job'а); `docker build` обоих образов OK; `helm lint` ×3 OK (через `alpine/helm`, helm локально не установлен).
- **Ревью-гейт: PASS** (1 MEDIUM + 4 LOW, не блокирующие). MEDIUM — required status checks в GitHub Branch Protection нельзя проверить/задать без `gh` → зафиксирован в BLOCKERS.md (действие Дениса в настройках репо). LOW закрыты сразу: `reuseExistingServer: !process.env.CI` (в CI vite всегда с нуля), `timeout-minutes: 20` на docker-job, чистка шаблонного выражения в комментарии `postgres/pvc.yaml` (тот же класс дефекта, что secret.yaml; `values.yaml` не шаблонизируется — там безопасно). LOW-остаток в backlog: передача jar-артефакта между jobs `backend`→`e2e` (сейчас двойная сборка — изолированно, но дороже по CI-минутам).
- DoD: все локальные репродукции шагов CI зелёные; frontend-job гейтит; e2e-job описан и локально воспроизводим; docker-job build-only + блокер зафиксирован. Reviewer PASS. Коммит `8d54a00`.

## Фаза 8 — Финальный quality gate и отчёт ✅
- **Чистка:** посторонний worktree `.claude/worktrees/strange-jang-f38717` снят (`git worktree remove`; изменения в нём — 100% CRLF-шум, проверено `--ignore-cr-at-eol`=пусто); ветка с 18 уникальными коммитами раннего прототипа сохранена — решение об удалении за Денисом (BLOCKERS.md). Мёртвого кода по diff прогона нет (LogSanitizer/ApplicationReadiness/aircraft-цепочка — все в прод-путях; grep-подтверждено).
- **Quality gate по diff всего прогона** (`bf07d31~1..HEAD`, 9 коммитов, code-reviewer): **PASS, 0 CRITICAL/HIGH**. Межфазные стыки (dedup V38 × rate limiting × readiness × leader election) согласованы; 1 MEDIUM информационный (двойной ready-флаг в LeaderElectionService — осознанный, задокументирован в коде), 2 LOW (пробел покрытия multi-hop XFF; отсутствие max-длины `search` — не DoS, LIKE линеен от messages, не от строки).
- **Доки синхронизированы:** ADR-0006 (rate limiting) + ADR-0007 (dedup V38) + актуализирован индекс `docs/adr/README.md` (0003–0005 не были внесены); README — продуктовый quickstart (8081, admin/admin); `installation.md` — реальные пути `/api/v1`, поле `token`, порт compose 8081, **переписана процедура замены демо-админа** (доки описывали несуществующий PATCH-эндпоинт смены пароля; реальная процедура — register нового ADMIN + toggle демо-учётки; отсутствие эндпоинта смены пароля — backlog); UAT-чеклист — порт 8081 (8 ссылок); DEMO_SCRIPT — порты запуска; ROADMAP — раздел «Итог прогона»; PROGRESS — закрытые backlog-пункты вычеркнуты (6 позиций).
- **License report v1.1:** +bucket4j 8.19.0 (Apache-2.0, 0 транзитивов) в runtime-раздел, +@playwright/test 1.61.1 (Apache-2.0, dev-only, браузеры не поставляются) — совместимо с Реестром.
- **PRODUCTION_READINESS_REPORT.md** создан: 12 пунктов чек-листа (все ✅), итог BLOCKERS (все внешние), ключевые числа.
- **Финальный verification loop:** FE — tsc 0, vitest 208/208, lint 0 errors/66 warnings (baseline), build OK; E2E 6/6 против живого стека; backend `bash mvnw -B verify` — **BUILD SUCCESS 10:54 мин, JaCoCo «All coverage checks have been met»** (906 тестов, Фаза 8 backend-тесты не меняла); SpotBugs/FindSecBugs security — 0; **Semgrep свежими правилами реестра нашёл 20 блокирующих находок** (правила добавлены в p/-пакеты после последнего зелёного CI — следующий пуш уронил бы CI-гейт): (1) 19× `github-actions-mutable-action-tag` — все actions в ci.yml запинены на полные commit-SHA (резолв через `git ls-remote`, тег в комментарии), (2) `allow-privilege-escalation-no-securitycontext` в `deploy/k8s/backend-deployment.yaml` — добавлен securityContext (allowPrivilegeEscalation:false, runAsNonRoot, drop ALL; Helm-шаблоны уже были захарднены в P8-1). Повторный Semgrep — exit 0.
- DoD: quality gate PASS; отчёт создан; доки синхронны; все контуры verification loop зелёные; рабочее дерево чистое после финального коммита.
